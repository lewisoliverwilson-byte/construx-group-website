---
title: "OpenSSF Scorecard: automated open source security scoring"
date: "2031-04-26"
excerpt: "The OpenSSF Scorecard runs automated security checks against a GitHub repository and produces a score from 0 to 10. It checks for branch protection, code review practices, dependency pinning, vulnerability disclosure, SAST tooling, and more. Running Scorecard in CI gives you a continuous signal on your repository's security posture — and a baseline to improve against over time."
author: "Lewis Wilson"
tag: "Build Log"
---

A security score for your repository. Automated, continuous, and actionable.

## Run against a public repo

```bash
# Install
brew install scorecard

# Score any public repo
scorecard --repo github.com/lewisoliverwilson-byte/construx-group-website

# JSON output for parsing
scorecard --repo github.com/lewisoliverwilson-byte/construx-group-website --format json
```

## Example output

```
Starting [CII-Best-Practices]
Starting [License]
Starting [Signed-Releases]
Starting [Branch-Protection]
Starting [Code-Review]
Starting [Dependency-Update-Tool]
Starting [Fuzzing]
Starting [Pinned-Dependencies]
Starting [SAST]
Starting [Vulnerabilities]

RESULTS
-------
Aggregate score: 6.2 / 10

Check Name          Score  Reason
────────────────────────────────────────────────────────────────────
Code-Review            8   8 out of last 10 commits reviewed
Branch-Protection      7   branch protection is configured on main
Vulnerabilities        9   0 unpatched vulnerabilities found
Pinned-Dependencies    4   22 out of 30 deps pinned to hash
SAST               5   CodeQL configured but not all workflows scanned
Signed-Releases        0   no signed releases found
Fuzzing                0   no fuzzing setup detected
CII-Best-Practices     0   no CII Best Practices badge found
```

## Add to GitHub Actions

```yaml
# .github/workflows/scorecard.yml
name: Scorecard supply chain security
on:
  branch_protection_rule:
  push:
    branches: [main, master]
  schedule:
    - cron: '0 0 * * 1'  # every Monday

permissions: read-all

jobs:
  analysis:
    name: Scorecard analysis
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      id-token: write

    steps:
      - uses: actions/checkout@v4
        with: { persist-credentials: false }

      - name: Run analysis
        uses: ossf/scorecard-action@v2.4.0
        with:
          results_file:   results.sarif
          results_format: sarif
          publish_results: true

      - name: Upload results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif
```

Results appear in GitHub's Security tab alongside CodeQL findings.

## Common improvements

**Pinned Dependencies (Pinned-Dependencies check)**

```yaml
# Bad — mutable tag
uses: actions/checkout@v4

# Good — pinned to commit hash
uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
```

Use Dependabot or Renovate to keep pins up to date automatically.

**Branch Protection (Branch-Protection check)**

```yaml
# .github/settings.yml (requires probot/settings)
branches:
  - name: main
    protection:
      required_pull_request_reviews:
        required_approving_review_count: 1
        dismiss_stale_reviews: true
      required_status_checks:
        strict: true
        contexts: [CI, Scorecard]
      enforce_admins: true
      restrictions: null
```

**SAST (SAST check)**

```yaml
# .github/workflows/codeql.yml
name: CodeQL
on: [push, pull_request]
jobs:
  analyze:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - uses: github/codeql-action/init@v3
        with: { languages: javascript-typescript }
      - uses: github/codeql-action/analyze@v3
```

**Signed Releases (Signed-Releases check)**

```bash
# Sign release artifacts with Sigstore
cosign sign-blob --yes dist/app.tar.gz --output-signature dist/app.tar.gz.sig
```

## Score targets

| Score | Posture                                        |
|-------|------------------------------------------------|
| 0-4   | High risk — branch protection, review missing  |
| 5-7   | Baseline — common security practices in place  |
| 8-9   | Solid — pinned deps, SAST, signed releases     |
| 10    | Full compliance — all checks passing           |

A score of 7+ covers the practices that matter most for most projects. Targeting 10 requires signed releases and fuzzing, which is appropriate for libraries but overkill for internal applications.
