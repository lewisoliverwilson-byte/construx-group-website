---
title: "shadcn/ui: copy-paste component primitives built on Radix"
date: "2030-07-06"
excerpt: "shadcn/ui is not a component library you install — it's a collection of components you copy into your project and own. Each component is built on Radix UI primitives (accessible by default) and styled with Tailwind CSS. You get full control: modify the source, adjust the styles, delete what you don't need. No dependency, no version lock, no fighting the library's opinions."
author: "Lewis Wilson"
tag: "Build Log"
---

Most component libraries are installed as packages. You get the components but you can't easily modify their internals — you're styling over the top of someone else's code. shadcn/ui inverts this: you run a CLI that copies the source directly into your project. The component becomes your code.

## Setup

```bash
npx shadcn-ui@latest init
```

This configures your project — sets up the `cn()` utility, installs Tailwind, and writes `components.json` with your color scheme preferences.

## Adding components

```bash
# Add individual components
npx shadcn-ui@latest add button
npx shadcn-ui@latest add dialog
npx shadcn-ui@latest add form
npx shadcn-ui@latest add select
npx shadcn-ui@latest add table
npx shadcn-ui@latest add toast

# Each command copies the component source into src/components/ui/
```

## Button

```tsx
// components/ui/button.tsx — in YOUR codebase, modify freely
import { Slot }   from '@radix-ui/react-slot';
import { cva, type VariantProps } from 'class-variance-authority';
import { cn }    from '@/lib/utils';

const buttonVariants = cva(
  'inline-flex items-center justify-center rounded-md text-sm font-medium transition-colors focus-visible:outline-none focus-visible:ring-2 disabled:pointer-events-none disabled:opacity-50',
  {
    variants: {
      variant: {
        default:     'bg-primary text-primary-foreground hover:bg-primary/90',
        destructive: 'bg-destructive text-destructive-foreground hover:bg-destructive/90',
        outline:     'border border-input bg-background hover:bg-accent',
        ghost:       'hover:bg-accent hover:text-accent-foreground',
        link:        'text-primary underline-offset-4 hover:underline',
      },
      size: {
        default: 'h-10 px-4 py-2',
        sm:      'h-9 rounded-md px-3',
        lg:      'h-11 rounded-md px-8',
        icon:    'h-10 w-10',
      },
    },
    defaultVariants: { variant: 'default', size: 'default' },
  },
);

export function Button({ className, variant, size, asChild = false, ...props }) {
  const Comp = asChild ? Slot : 'button';
  return <Comp className={cn(buttonVariants({ variant, size }), className)} {...props} />;
}
```

## Dialog (modal)

```tsx
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter,
} from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';

function DeleteDialog({ onConfirm, open, onOpenChange }: { onConfirm: () => void; open: boolean; onOpenChange: (o: boolean) => void }) {
  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Delete post</DialogTitle>
          <DialogDescription>
            This action cannot be undone. The post will be permanently deleted.
          </DialogDescription>
        </DialogHeader>
        <DialogFooter>
          <Button variant="outline" onClick={() => onOpenChange(false)}>Cancel</Button>
          <Button variant="destructive" onClick={onConfirm}>Delete</Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

## Form with react-hook-form + zod

```tsx
import { useForm }  from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z }        from 'zod';
import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from '@/components/ui/form';
import { Input }    from '@/components/ui/input';
import { Button }   from '@/components/ui/button';

const schema = z.object({
  email: z.string().email(),
  name:  z.string().min(1, 'Name is required'),
});

type FormData = z.infer<typeof schema>;

function SignupForm({ onSubmit }: { onSubmit: (data: FormData) => Promise<void> }) {
  const form = useForm<FormData>({ resolver: zodResolver(schema) });

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
        <FormField control={form.control} name="name" render={({ field }) => (
          <FormItem>
            <FormLabel>Name</FormLabel>
            <FormControl><Input placeholder="Lewis Wilson" {...field} /></FormControl>
            <FormMessage />
          </FormItem>
        )} />
        <FormField control={form.control} name="email" render={({ field }) => (
          <FormItem>
            <FormLabel>Email</FormLabel>
            <FormControl><Input type="email" placeholder="lewis@construxgroup.io" {...field} /></FormControl>
            <FormMessage />
          </FormItem>
        )} />
        <Button type="submit" disabled={form.formState.isSubmitting}>
          {form.formState.isSubmitting ? 'Saving…' : 'Sign up'}
        </Button>
      </form>
    </Form>
  );
}
```

## Theming

```css
/* globals.css — CSS variables drive all component colours */
:root {
  --background: 0 0% 100%;
  --foreground: 222.2 47.4% 11.2%;
  --primary: 24.6 95% 53.1%;  /* orange */
  --primary-foreground: 210 40% 98%;
  --muted: 210 40% 96.1%;
  --border: 214.3 31.8% 91.4%;
  --ring: 24.6 95% 53.1%;
}

.dark {
  --background: 224 71% 4%;
  --foreground: 213 31% 91%;
  --primary: 24.6 95% 53.1%;
  --border: 216 34% 17%;
}
```

## shadcn/ui vs Radix alone vs Chakra vs MUI

shadcn/ui: you own the source, full customisation, Tailwind-native. Radix alone: accessible primitives, unstyled — you write all the CSS. Chakra: styled component library you install as a package, less customisation. MUI: Material Design, complete, opinionated — strong for enterprise admin UI. For most Next.js projects with Tailwind, shadcn/ui is the pragmatic choice — Radix accessibility without Radix boilerplate, and ownership of every line of component code.
